const number = Number(prompt("몇 명이 참가하나요?"));

/**
 * 태그.addEventListener('이벤트 이름', 리스너 함수(콜백 함수));
 */
document.querySelector("input").addEventListener("input", function () {});
document.querySelector("button").addEventListener("click", function () {});