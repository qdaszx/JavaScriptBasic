/**
 * 프로퍼티 리스트 (ES3)
 * 이름 - 개요
 * new Object() - 파라미터 데이터 타입의 인스턴스 생성
 * Object() - Object 인스턴스 생성
 *
 * Obect.prototype
 * constructor - 생성자
 * valueOf() - 프리미티브 값 반환
 * hasOwnPropert() - 프로퍼티 소유 여부 반환
 * propertyIsEnumerable() - 프로퍼티 열거 여부 반환
 * isPrototypeOf() - prototype의 존재 여부 반환
 * toString() - 문자열로 변환
 * toLocaleString() - 지역화 문자열로 변환
 */